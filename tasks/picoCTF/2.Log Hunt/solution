к заданию прилагается файл с логами сервера который время от времени выводит части флага сначало посмотрю на структуру
после сделаю вывод возможно ли как то выделить строки с флагом
INFO FLAGPART: picoCTF{us3_
первая строка в которой часть флага
сразу видно что можно через ctrl + F найти части но лучше напишу скрипт который отсечет все кроме этих строк ну либо
можно сделать через grep
сделал скриптом можно было доделать чтобы выводило только нужную часть флага да и в одну строку цельно но решил не париться
черезмерное усложнение получится
picoCTF{us3_y0urlinux_sk1lls_cedfa5fb}
если делать через греп выглядело бы примерно так cat server.log | grep "FLAGPART" ну или grep "FLAGPART" server.log
если бы была маска можно было бы использовать -F конечно можно и сейчас в таком виде grep -F "FLAGRART*" но это не имеет смысла
мы и так не обращаем внимания на символы после т.к. это избыточно
